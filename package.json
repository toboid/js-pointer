{
  "name": "json-pointer",
  "version": "0.0.1",
  "description": "",
  "main": "lib/index.js",
  "scripts": {
    "lint": "node_modules/standard/bin/cmd.js",
    "precompile": "rm -f dist/*",
    "compile": "node_modules/babel/bin/babel.js lib --out-dir dist",
    "pretest": "npm run compile",
    "test": "node_modules/mocha/bin/mocha --harmony",
    "watch": "node_modules/watch/cli.js 'npm run lint && npm test' src test",
    "prepublish": "npm compile"
  },
  "repository": {
    "type": "git",
    "url": "git@bitbucket.org:toby_s/json-pointer.git"
  },
  "keywords": [
    "json-pointer",
    "json-reference",
    "json-schema",
    "node"
  ],
  "author": "Toboid",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "babel": "^5.8.21",
    "chai": "^3.0.0",
    "mocha": "^2.2.5",
    "standard": "^5.2.2",
    "watch": "^0.16.0"
  },
  "standard": {
    "globals": [ "describe", "it" ]
  }
}
